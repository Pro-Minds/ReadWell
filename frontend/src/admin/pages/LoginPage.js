import React, { useState } from "react";
import LoginForm from "../auth/LoginForm";

const LoginPage = () => {
    return (
        <div>
            <h1>Admin Login</h1>
            <LoginForm />
        </div>
    );
}

export default LoginPage;